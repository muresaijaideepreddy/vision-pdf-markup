# These are examples. Set environment variables in your shell; this file is not loaded.
OPENAI_API_KEY=
MARKUPDOC_MODEL=
# Only change this if using a known compatible service.
OPENAI_BASE_URL=https://api.openai.com/v1
